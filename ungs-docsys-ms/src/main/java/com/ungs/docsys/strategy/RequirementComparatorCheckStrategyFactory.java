package com.ungs.docsys.strategy;

import java.util.List;
import java.util.Map;
import java.util.stream.Collectors;

public class RequirementComparatorCheckStrategyFactory {

/*    private final Map<String, RequirementComparatorCheckStrategy> strategies;

    public RequirementComparatorCheckStrategyFactory(List<RequirementComparatorCheckStrategy> strategyList) {
        this.strategies = strategyList.stream().collect(
                Collectors.toMap(RequirementComparatorCheckStrategy:)
        )
    }*/
}
